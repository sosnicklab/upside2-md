doc/
run/
bin/
data/
doc/
*~
*.swp
*.vtf
*.pyc
a.out
*.so
*.dSYM/
*.o
*.i
*.s
.clang_complete
CMakeCache.txt
CMakeFiles/
*.bak
*.DS_Store
._*
.ycm_extra_conf.py
compile_commands.json
source.sh
**inputs/
**outputs/
.vscode/*
.venv/
.claude/scheduled_tasks.lock
parameters/dryMARTINI/*.h5
*.pdf
*scratchpad*
backup/*
training/*
# ...but keep the training infrastructure itself; run directories and data stay ignored.
!training/README.md
!training/*.py
!training/*.sh
!training/*.sbatch
!training/pdb_list

# Glycine-map scripts kept out of the repo (2026-09-19).  rama_gly_gradient is NOT here: it
# moved to training/ and stays tracked, because training/ConDiv.py imports it and a clean clone
# has to be able to run the trainer.
py/build_rama_from_awh.py
training/verify_gly_gradient.py

*.bak_*
