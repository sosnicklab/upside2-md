## Progress Log

- 2026-03-11: Initialized task tracking files for the `ConDiv_symlay` Slurm workflow redesign.
- 2026-03-11: Inspected `ConDiv_symlay/run_remote.sh`, `ConDiv_symlay/training_control.py`, and `ConDiv_symlay/ConDiv_mem.py`; confirmed the current remote workflow is a single Slurm allocation with internal worker fan-out.
- 2026-03-11: Chose a staged design: generate one `sbatch` file per protein simulation, submit a dependent FF-update Slurm job with `afterany`, and let that update job submit the next training round.
- 2026-03-11: Refactored `ConDiv_symlay/ConDiv_mem.py` into reusable minibatch helpers so staged Slurm rounds and the legacy in-process flow share the same FF-update logic.
- 2026-03-11: Added `ConDiv_symlay/slurm_round.py`, `ConDiv_symlay/submit_remote_round.sh`, and `ConDiv_symlay/run_remote_update.sh` to stage per-target job files, submit them, finalize the round, and submit the next round.
- 2026-03-11: Updated `ConDiv_symlay/run_init.sh` and `ConDiv_symlay/README.md` to point to the separate-job Slurm workflow and document the legacy single-allocation mode separately.
- 2026-03-11: Verified Python syntax, shell syntax, and local round staging with a temporary checkpoint copy; could not verify live Slurm submission or full FF finalization without cluster access and generated divergence files.
- 2026-03-11: User corrected the Slurm design: the simulation side must be a single array `sbatch` file per round, with one array task per protein simulation. Refactor in progress.
- 2026-03-11: Refactored `ConDiv_symlay/slurm_round.py` to stage one `simulate_array.sbatch` file per round, submit one array job, and dispatch tasks by `SLURM_ARRAY_TASK_ID` through the round manifest.
- 2026-03-11: Re-verified Python syntax, shell syntax, and local no-submit staging; confirmed the generated simulation script is a single job-array sbatch file.
- 2026-03-11: Added a preflight file check to `ConDiv_symlay/submit_remote_round.sh` so cluster checkouts missing `slurm_round.py` and related workflow files fail with a clear sync error.
- 2026-03-13: Investigated the failed `epoch_00_minibatch_00` cluster round and confirmed the `upside` simulator completes, but the worker aborts before writing `*.divergence.pkl`, causing the dependent update job to fail with `All jobs failed`.
- 2026-03-13: Patched `ConDiv_symlay/ConDiv_mem.py` so the native `compute_divergence()` step runs in an isolated helper subprocess that flushes its pickle output and exits without interpreter teardown.
- 2026-03-13: Verified the updated `ConDiv_symlay/ConDiv_mem.py` with `python3 -m py_compile` and a project-environment import check after sourcing `.venv/bin/activate` and `source.sh`.
- 2026-03-13: Replayed the downloaded `ConDiv_symlay/test_dimer3/epoch_00_minibatch_00` outputs through the new helper path; found that some targets still abort on a full 300-frame batch even though smaller halves succeed.
- 2026-03-13: Patched the post-processing path again to skip native engine free inside the helper subprocess and recursively bisect failing divergence batches until they succeed.
- 2026-03-13: Re-ran the helper against all 15 downloaded targets in `epoch_00_minibatch_00`; every target completed contrast evaluation successfully, and `python3 -m py_compile ConDiv_symlay/ConDiv_mem.py py/upside_engine.py` passed.
- 2026-03-13: Added update-time recovery so `slurm_round.py finalize-round` rebuilds missing `*.divergence.pkl` files from existing `.run.*.h5` outputs before the FF update.
- 2026-03-13: Normalized empty recovered inner-gradient blocks to the checkpoint parameter shapes, which resolved a finalize-time broadcast failure during pair regularization on the downloaded round.
- 2026-03-13: Verified the end-to-end recovery path on the downloaded `ConDiv_symlay/test_dimer3/epoch_00_minibatch_00` data: all 15 divergence files were rebuilt and `finalize_minibatch_from_outputs()` completed successfully.
- 2026-03-13: Re-ran verification from a fresh temporary copy of `ConDiv_symlay/test_dimer3` after deleting all 15 recovered `*.divergence.pkl` files. Patched the copied checkpoint/manifest to local paths and ran `python3 ConDiv_symlay/slurm_round.py finalize-round --round-manifest <temp manifest>` end-to-end; it rebuilt all 15 divergence files, wrote `epoch_00_minibatch_00/checkpoint.pkl`, updated `training_progress.jsonl` / `training_status.json`, and exited cleanly with auto-resubmit disabled.
- 2026-03-13: Re-verified the same downloaded `epoch_00_minibatch_00` failure on a fresh local temp copy at `/private/tmp/condiv_symlay_replay.7XBGe0`; `python3 ConDiv_symlay/slurm_round.py finalize-round --round-manifest <patched temp manifest>` recovered all 15 divergence files again, produced `Median RMSD 1.10 9.44` / `Median COM 0.71 0.94`, wrote `checkpoint.pkl`, and appended one progress record with `n_success=15`.
- 2026-03-13: User corrected the earlier assumption: `ConDiv_symlay/slurm-46899622.out` is already from the current code. Reproduced the live helper instability locally and confirmed that current `recover_missing_divergence_outputs()` still provokes child-process crashes and macOS popup dialogs during normal recovery.
- 2026-03-13: Patched `ConDiv_symlay/ConDiv_mem.py` to force the divergence helper subprocess to single-threaded `OMP`/`BLAS` settings, lowered the proactive batch limit default from `64` to `8`, and switched the helper launcher to the new lightweight `ConDiv_symlay/divergence_helper.py` entrypoint.
- 2026-03-13: Patched `py/upside_engine.py` so `Upside.__init__` reads `/input/pos` and `/input/sequence` via `h5py` instead of `PyTables`; this removes one class of repeated-helper HDF5 close failures, but helper aborts still reproduce on `2oar` under the current native derivative path.
- 2026-03-13: Narrowed the remaining failure to genuine native instability that can still occur even on single-frame helper calls. Located a reproducible first failing frame in one `2oar` replay (`finite-equil` frame index `173`, helper exit `-5`), so popup-free local recovery is not fully resolved yet.
- 2026-03-13: Identified the native root cause in `src/membrane_potential.cpp`: `MembraneCBPotential::get_param_deriv()` and `MembraneSurfCBPotential::get_param_deriv()` both wrote right-side spline derivative contributions past the end of their `deriv(np)` buffers via `shift + np`, corrupting helper-process memory during membrane gradient extraction.
- 2026-03-13: Patched those two out-of-bounds writes, rebuilt `obj/upside`, `obj/upside_engine`, and `obj/libupside.dylib`, and verified the previously failing `2oar` single-frame helper probe (`finite-equil` frame `173`) passed 20/20 times after the native fix.
- 2026-03-13: Proved simulation parity for the `src/membrane_potential.cpp` fix by building a temporary pre-patch `upside` binary, running old and new binaries on identical stripped copies of `2oar.run.1.h5` with the same seed and short duration, and comparing shared `/output` arrays; every dataset matched exactly, including `pos`, `potential`, `kinetic`, and `hbond`.
- 2026-03-13: Re-ran the membrane-analysis stress targets (`2oar`, `3ukm`, `5vb2`) through `ConDiv_mem._compute_divergence_isolated(...)` after the native fix; all completed with zero helper retries at the restored `CONDIV_DIVERGENCE_BATCH_LIMIT=64`.
- 2026-03-13: Re-ran `python3 ConDiv_symlay/slurm_round.py finalize-round --round-manifest <patched temp manifest>` on a fresh temp copy of `ConDiv_symlay/test_dimer3` after deleting all divergence files. The recovery rebuilt all 15 `*.divergence.pkl` files with no `RECOVER_DIVERGENCE_FAIL` or `MISSING_DIVERGENCE`, wrote `checkpoint.pkl`, and appended one progress record with `n_success=15`.
- 2026-03-13: Inspected `ConDiv_symlay/test_dimer3/epoch_00_minibatch_00/slurm/update-46924605.out` and confirmed the current failure is a checkpoint bootstrap error: `cp.load()` raises `ModuleNotFoundError: No module named 'numpy._core.numeric'` before analysis/training starts.
- 2026-03-13: Confirmed the bundled `ConDiv_symlay/test_dimer3/initial_checkpoint.pkl` contains the NumPy 2 module path `numpy._core.numeric`, and `ConDiv_mem.load_checkpoint()` still uses a raw `cp.load(fh)`.
- 2026-03-13: Patched `ConDiv_mem.load_checkpoint()` to use a compatibility unpickler that remaps `numpy._core...` globals to `numpy.core...` during checkpoint load so locally generated NumPy 2 checkpoints can bootstrap on older cluster NumPy environments.
- 2026-03-13: Verified the checkpoint portability fix with `python3 -m py_compile ConDiv_symlay/ConDiv_mem.py`, a normal `cd.load_checkpoint('ConDiv_symlay/test_dimer3/initial_checkpoint.pkl')` run, and a simulated `ModuleNotFoundError` for `numpy._core.numeric`; both load paths returned the expected training state (`epoch=0`, `i_mb=0`).
